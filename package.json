{
  "name": "pi-jev",
  "version": "0.3.0",
  "description": "A Pi extension for Jev-guided context selection",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "context-management",
    "jev"
  ],
  "pi": {
    "extensions": [
      "./src/extension.ts"
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json --noEmit && vitest run test",
    "dev": "pi -e ./src/extension.ts",
    "test": "vitest run test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "0.6.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@types/node": "22.19.19",
    "tsx": "4.20.6",
    "typebox": "1.3.7",
    "typescript": "5.9.3",
    "vitest": "4.1.0"
  }
}
