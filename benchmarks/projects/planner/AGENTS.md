# Benchmark project rules

- Use Node.js and `npm test`.
- Do not add dependencies or modify tests.
- Preserve the API documented in README.md.
- Write the final implementation summary to `REPORT.md`.
